# Public — safe to expose to the browser (Astro embeds PUBLIC_ vars in the client bundle)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Secret — server-only, never prefix with PUBLIC_, never commit real values
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
